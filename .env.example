# Copy this file to .env before running locally
# cp .env.example .env

# Network / environment label
NETWORK=development

# RPC endpoint (local or remote)
RPC_URL=http://localhost:8545

# Optional API key / token (if your setup requires one)
API_KEY=

# Logging level (debug | info | warn)
LOG_LEVEL=info
